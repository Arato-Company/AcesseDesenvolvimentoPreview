import { PageShell } from "@/components/PageShell";

export default function EmpresaVitrinePage() {
  return (
    <PageShell
      eyebrow="Web/03 · Empresa"
      title="Vitrine de Profissionais"
      description="Grid de cards de candidatos com filtros (cidade, area, nivel). Star do produto — onde a curadoria aparece como selo dourado."
    />
  );
}
