import { PageShell } from "@/components/PageShell";

export default function EmpresaVagaNovaPage() {
  return (
    <PageShell
      eyebrow="Web/05 · Empresa"
      title="Publicar Vaga"
      description="Stepper de publicacao de vaga — basico, requisitos, beneficios, revisao."
    />
  );
}
