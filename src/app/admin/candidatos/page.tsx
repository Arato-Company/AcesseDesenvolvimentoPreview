import { PageShell } from "@/components/PageShell";

export default function AdminCandidatosPage() {
  return (
    <PageShell
      eyebrow="Web/11 · Admin"
      title="Admin · Candidatos"
      description="Tabela de todos os candidatos — filtro por status, plano, area e cidade."
    />
  );
}
