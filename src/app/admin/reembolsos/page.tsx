import { PageShell } from "@/components/PageShell";

export default function AdminReembolsosPage() {
  return (
    <PageShell
      eyebrow="Web/15 · Admin"
      title="Admin · Reembolsos"
      description="Fila de pedidos de reembolso proporcional (candidato que fechou vaga atraves do SaaS antes do ciclo de 5 meses)."
    />
  );
}
