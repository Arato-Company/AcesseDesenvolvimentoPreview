import { PageShell } from "@/components/PageShell";

export default function CandidatoCadastroPage() {
  return (
    <PageShell
      eyebrow="Mobile/02 · Candidato"
      title="Onboarding Cadastro"
      description="Fluxo de cadastro do candidato em passos. Mobile-first — desktop usa o mesmo template em coluna unica."
    />
  );
}
