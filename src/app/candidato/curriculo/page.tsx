import { PageShell } from "@/components/PageShell";

export default function CandidatoCurriculoPage() {
  return (
    <PageShell
      eyebrow="Mobile/06 · Candidato"
      title="Curriculo Gerado"
      description="Visualizacao do curriculo profissional do candidato — versao impressao (Start) ou PDF profissional (Destaque/Premium)."
    />
  );
}
