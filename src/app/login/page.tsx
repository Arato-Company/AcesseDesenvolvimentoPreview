import { PageShell } from "@/components/PageShell";

export default function LoginPage() {
  return (
    <PageShell
      eyebrow="Web/01 + Mobile/01 · Publico"
      title="Entrada Login Dual"
      description="Tela de entrada com dois caminhos: candidato e empresa. Versao mobile e a referencia desta mesma rota (sem fork de rota)."
    />
  );
}
